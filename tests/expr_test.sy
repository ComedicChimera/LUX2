func Main() int {
    $x = 2;

    $m: map[char, int] = {'a': 2};

    template typename<type{int, str}> {
        $y = 5;
    }

    $z = for(m|k| => int(k) * 2);

    $b = x{2, 3};

    lock $a = 43;

    $arr = array[int, 4](["a", 2, 3, 4]);
}